import { Grid, Typography } from '@mui/material'
import React from 'react'

const Note = () => {
  return (
    <Grid container justifyContent={'center'} m={1}>
        <Typography variant='subtitle2' color='error'>Note : The below shown case images are demo images,every case that shown below is available in all models of your choosen brand</Typography>
    </Grid>
  )
}

export default Note